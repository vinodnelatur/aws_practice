Parameters:
    VPCname:
      Description: Create a VPC with the following name
      Type: String
Resources:
    MyVpc:
      Type: AWS::EC2::VPC
      Properties:
        CidrBlock: 10.0.0.0/16
    Subnet1:
      Type: AWS::EC2::Subnet
      Description: Create Subnet1 for placing EC2 server1
      Properties:
        VpcId: !Ref MyVpc
        CidrBlock: 10.0.1.0/24
        AvailabilityZone: "ap-southeast-2a"
        Tags:
          Keys: Application
          Value: !Ref "AWS::StackId"
    Subnet2:
      Type: AWS::EC2::Subnet
      Description: Create Subnet2 for placing EC2 server2
      Properties:
        VpcId: !Ref MyVpc
        CidrBlock: 10.0.2.0/24
        AvailabilityZone: "ap-southeast-2b"
        Tags:
          Keys: Application
          Value: !Ref "AWS::StackId"

<internet_gateway>
<attach_gateway>
<route_tables>
<subnet_to_route_table_association>
<Inbound_acl_entry>
<outbound_acl_entry>
<load_balancer> <do we need Auto scale??? >
<server_group>
<launch_config>
<ec2_instances_with_ebs*2>
    <launch with metadata configuration of a web server>
<ec2_instances_secruty gorup>
<route53_health_check>
<cloudwatch_metrics_cpu>


          
        
      